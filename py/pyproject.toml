[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mds-core"
version = "0.17.0"
description = "MDS - Markdown Document Schema: validate Markdown against .mds contracts from files, strings or streams - deterministic structure, types, constraints and LLM-readable semantic expectations with structured diagnostics."
readme = "README.md"
license = "MIT"
authors = [
  { name = "Lorenz Sommer", email = "59441867+sommelo1@users.noreply.github.com" },
]
requires-python = ">=3.10"
dependencies = []
keywords = ["markdown", "schema", "validation", "llm", "ai", "document", "contract", "gfm"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Software Development :: Quality Assurance",
  "Topic :: Text Processing :: Markup :: Markdown",
]

[project.urls]
Homepage = "https://github.com/sommelo1/mds"
Repository = "https://github.com/sommelo1/mds"
Issues = "https://github.com/sommelo1/mds/issues"

[project.scripts]
mds = "mds.cli:console_main"

[tool.setuptools]
packages = ["mds", "mds.skills"]

[tool.setuptools.package-data]
mds = ["skills/*.md"]
