# mds
MDS turns Markdown from an informal document format into a contract-driven, testable and machine-verifiable document system. A .mds contract defines the deterministic structure (sections, types, constraints) and native semantic expectations via expect:: directly readable by humans and LLMs, optionally evaluated by extensions.
